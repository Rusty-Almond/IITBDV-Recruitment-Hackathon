# Changes done

- Added basic physics where a force acts on the car in the x direction only(but along y will also work).
- Basic modelling of friction like viscous force because N will change along X and Y according to F which is not calculated. When velocity becomes too small stop.
- Calculates the speed, velocity at each iteration by euler's method.